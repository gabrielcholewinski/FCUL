int levantamento(quantia) {
    int saldo;
    saldo = //ler saldo da zona
            // de memória
    saldo -= quantia;
    usleep(1);
    //escrever  saldo  na  zona  de  memória
    return saldo;
}

int deposito(quantia) {
    int saldo;
    saldo = //ler saldo da zona
            // de memória
    saldo += quantia;
    usleep(1);
    //escrever saldo na zona de memória
    return saldo;
}

